server:
  port: 8007
  servlet:
    context-path: /api

spring:
  application:
    name: ancillary-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_ancillary_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true