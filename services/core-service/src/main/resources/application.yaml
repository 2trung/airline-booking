server:
  port: 8003
  servlet:
    context-path: /api

spring:
  application:
    name: core-service

  datasource:
    url: jdbc:mysql://localhost:3306/airline_core_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
